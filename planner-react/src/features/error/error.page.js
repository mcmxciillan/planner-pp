export default function ErrorPage() {
    return (
        <div>
            <h1>Sorry, this has never happened before...</h1>
            <p>Try starting over and we'll see if we can't get things right</p>
        </div>
    )
}