export default function ErrorPage() {
    return (
        <div>
            <h1 className="text-3xl">Sorry, this has never happened before...</h1>
            <p className="text-xl">Try starting over and we'll see if we can't get things right</p>
        </div>
    )
}