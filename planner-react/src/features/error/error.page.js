export default function ErrorPage() {
    return (
        <div>
            <h1 className="text-3xl font-bold text-center">Sorry, this has never happened before...</h1>
            <p className="text-center">Try starting over and we'll see if we can't get things right</p>
        </div>
    )
}